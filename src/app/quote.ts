export class Quote {
    showInfo: boolean;
    constructor(public id:number,public name:string,public quote-title:string,public quote-info:string, public datePosted:Date,public upvote:number,public downvote:number)
    this.showInfo=false;
}
